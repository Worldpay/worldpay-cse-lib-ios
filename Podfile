platform :ios, '10.0'

use_frameworks!
inhibit_all_warnings!

install! 'cocoapods', :share_schemes_for_development_pods => true

target 'Dummy' do
end

pod 'OpenSSL-Universal', :path => '.'
pod 'WorldpayCSE', :path => '.', :testspecs => ['Tests']